// export enum LightStatus {
//     LIGHT,
//     DARK,
// }

export enum BgColors {
    DARK = "#102027",
    LIGHT = "#fafafa",
}

export enum Storage {
    LIGHT_MODE = "lightMode",
    ORDER_BY = "orderBy",
    TOKEN = "token",
}
